{
  "name": "reactive-chat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint:server": "cd server && bun run lint",
    "format:server": "cd server && bun run format",
    "lint:client": "cd client && bun run lint",
    "format:client": "cd client && bun run format"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  },
  "lint-staged": {
    "server/src/**/*.ts": [
      "bun --cwd server prettier --write",
      "bun --cwd server eslint --fix"
    ],
    "client/src/**/*.{ts,tsx}": [
      "bun --cwd client prettier --write",
      "bun --cwd client eslint --fix"
    ]
  },
  "type": "module"
}
